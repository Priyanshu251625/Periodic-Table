# Periodic-Table
This is a simple periodic table desigh which allows for searching of elements by name symbol and categories i.e. type also the elements tiles pop out on hover and each tile redirects to the element's wikipedia page on click
